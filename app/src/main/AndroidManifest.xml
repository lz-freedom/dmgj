<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.github.hellowlrld">

    <uses-sdk tools:overrideLibrary="scut.carson_ho.searchview" />

    <!-- 为解决添加的库兼容的最低版本与项目设置的版本不同 -->

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.github.dmgj.activity_welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.github.dmgj.MainActivity" />
        <activity android:name="com.example.github.dmgj.activity_sjdm" />
        <activity android:name="com.example.github.dmgj.activity_qtdm" />
        <activity android:name="com.example.github.dmgj.activity_xuanze" />
        <activity android:name="com.example.github.dmgj.activity_ss"></activity>
    </application>

</manifest>